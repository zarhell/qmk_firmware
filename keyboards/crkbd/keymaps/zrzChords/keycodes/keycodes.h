#pragma once

enum custom_keycodes {
    // Funciones personalizadas
    LLOCK = SAFE_RANGE,
    SELWORD,

    // --- GIT Combos ---
    GIT_STATUS,
    GIT_COMMIT,
    GIT_PULL,
    GIT_PUSH,
    GIT_LOG,
    GIT_ADD,
    GIT_CHECKOUT,
    GIT_RESTORE,
    GIT_REVERT,
    GIT_STASH,

    // --- Symbol Combos ---
    LEFT_BRACKET,
    LEFT_CURLY_BRACE,
    LEFT_PAREN,
    LEFT_SLSH,
    LEFT_BSLS,
    LEFT_PIPE,
    LEFT_CIRC,
    LEFT_LOWER_THAN,
    LEFT_GREATHER_THAN,
    LEFT_GRV,
    LEFT_TILD,
    LEFT_HASH,
    LEFT_PERC,
    LEFT_EXLM,
    LEFT_QUES,
    LEFT_DLR,
    LEFT_AT,
    LEFT_AMPR,
    LEFT_ASTR,

    numSLSH,
    numBSLS,
    numPIPE,
    numCIRC,
    numLT,
    numGT,
    numGRV,
    numTILD,
    numHASH,
    numPERC,
    numEXLM,
    numQUES,
    numDLR,
    numAT,
    numAMPR,
    numASTR,

    // --- Media Combos ---
    MEDIA_PLAY_PAUSE,
    MEDIA_VOL_UP,
    MEDIA_VOL_DOWN,
    MEDIA_MUTE,
    MEDIA_NEXT_TRACK,
    MEDIA_PREV_TRACK,

    // --- Misc Combos ---
    EMAIL_GMAIL,
    EMAIL_WORK,
    USERNAME,
    PASSWORD,
    SIGNATURE,

    // --- Spanish Combos ---
    TILDE_A,
    TILDE_E,
    TILDE_I,
    TILDE_O,
    TILDE_U,
    TILDE_N,
    ENIE,
    EXCLAM,
    QUESTION,

};