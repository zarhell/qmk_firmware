#include "combos_misc.h"

// Definiciones de combinaciones (4 teclas para evitar disparos accidentales)
const uint16_t PROGMEM email_gmail_combo[] = { KC_A, KC_S, KC_D, KC_F, COMBO_END }; // asdf
const uint16_t PROGMEM email_work_combo[]  = { KC_H, KC_J, KC_K, KC_L, COMBO_END }; // hjkl
const uint16_t PROGMEM username_combo[]    = { KC_U, KC_S, KC_R, COMBO_END };
const uint16_t PROGMEM password_combo[]    = { KC_P, KC_W, COMBO_END };
